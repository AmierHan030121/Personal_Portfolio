# 个人作品集网站重构执行提示词（安全修订版）

> 用途：在当前仓库内继续维护 AmierHan 的数据分析与研究作品集。目标是交付可维护、可访问、可部署的 React 网站源码，并将通过验收的版本推送到 `origin/main20260524`。

## 任务目标

你是一名产品设计与前端工程师。请将现有作品集整理为一个面向 HR 和面试官的中文 React/Vite/TypeScript 网站，突出数据分析、商业智能、可视化和研究建模能力。

重要边界：

- 只使用仓库中已有且来源明确的头像、作品截图、PDF 和文字；不虚构项目指标、客户、经历、奖项或研究结论。
- 当前版本不包含用户制作的视频。不要添加视频、视频封面、视频播放器、远程视频链接、第三方 iframe 或未经确认的媒体。
- 作品预览只允许使用本地图片和本地 PDF。PDF 预览失败时必须提供打开原始文件的备用链接。
- 不临摹任何具体网站、作者、品牌、角色、Logo、照片、字体或文案。参考截图只能用于归纳通用的布局和信息层级，最终视觉必须原创，并服务于数据分析身份。
- Figma、Canva、图片生成服务均为可选工具。没有可调用工具、文件或授权时，不得声称已调用或已完成；使用本地 `docs/design/` 设计规范作为替代。
- 不删除、覆盖或重命名原始作品和个人材料。完整简历只供维护参考，不默认进入生产构建或公开链接。

## 当前项目事实

工作目录：`C:\Users\Dell\Desktop\杂项\简历\研究生期间简历\个人作品集网站搭建`

主要结构：

- `src/app/`：应用入口和路由
- `src/components/`：导航、作品卡片、媒体预览、首页工作台组件
- `src/pages/`：首页、看板、研究、关于我、项目详情
- `src/content/`：个人信息和项目数据
- `src/styles/`：设计 token、布局和组件样式
- `作品/`：作品原图与 PDF
- `assets/`：头像、预览图和经过确认可以公开的本地素材
- `docs/design/`：无法使用设计工具时的本地设计规范

当前作品数据包括电商增长、B 站 UP 主监控、K12 经营分析、国际物流结算、二手帆船定价、省域研发与知识产权转化、算电协同调度等项目。以仓库中的 `src/content/projects.ts` 为准，不自行增加事实。

## 已确认的设计方向 A

命题：数据分析师的个人工作台。

- 采用冷白、浅灰蓝、少量蓝色强调的浅色背景，并用细网格表达分析坐标感。
- 首屏同时表达“我是谁、做什么、精选作品在哪里”，但不把内容做成一张图片或空洞 Hero。
- 作品截图和 PDF 首页是主要视觉资产，贴纸或插画只能作为辅助，不能抢占作品阅读空间。
- 中文正文使用清晰的无衬线字体；手写感字体只用于英文姓名或短签名；数据标签可使用等宽字体。
- 桌面端可使用受容器约束的拼贴布局；手机端必须重排为单列信息流，不得横向溢出。
- 动效只用于层级、悬停反馈和导航状态，支持 `prefers-reduced-motion`，不使用大面积粒子、3D 或鼠标追踪特效。

## 实施要求

1. 先检查 `git status`、现有路由、资源引用和构建脚本，再编辑文件。保留与本任务无关的用户改动。
2. 项目内容使用 TypeScript 数据模型维护。新增项目优先新增一条数据和素材，不复制整页 JSX。
3. 使用语义 HTML、清晰的标题层级、跳过导航、键盘焦点、图片 `alt`、稳定媒体尺寸和失败兜底。
4. 站点使用 `HashRouter` 以兼容静态托管刷新，不依赖运行时后端、数据库、登录或外部 CDN。
5. 保持作品入口真实可用：首页 → 项目列表/项目详情 → 图片或 PDF 预览 → 返回列表/筛选。
6. 不把测试截图、日志、完整简历、压缩包或未确认的参考素材复制到生产构建；临时 QA 文件留在工作区即可，不要提交。

## 工具使用规则

- 需要视觉检查时，优先使用本地 `view_image` 和可用的浏览器工具。
- 如果浏览器插件不可用，可使用仓库已有的 Playwright/浏览器方案，并记录具体原因；不能只凭构建成功声称视觉验收通过。
- 只有在本地素材不足且用户明确同意时，才生成独立的原创非特定 IP 图片；生成图不能替代可访问的 HTML/React UI。
- 不访问或抓取外部网站来复制内容，不把外部站点的图片、视频、字体或代码带入仓库。

## 验收清单

至少验证：

- `npm run typecheck`
- `npm run test`
- `npm run build`
- 本地开发服务器首页、看板、研究、关于我和一个项目详情路由
- 1440 宽桌面与 390 宽手机的首屏截图
- 图片/PDF 加载失败时的备用链接
- 控制台无新增运行时错误，页面无明显裁切、重叠、溢出或不可读文字
- 页面中不存在 `<video>`、`.mp4`、远程视频地址或未经确认的第三方媒体

## Git 交付边界

- 当前目标分支固定为 `main20260524`。
- 提交前只暂存本次网站改造涉及的源码、必要本地素材、设计规范和本提示词；不暂存完整简历、临时截图、日志、压缩包、密钥或无关改动。
- 使用普通非交互式提交并推送到 `origin/main20260524`。禁止强制推送、改写历史、删除远端分支或操作 EdgeOne 控制台。
- 推送后核对本地 HEAD 与 `origin/main20260524` 指向同一提交，并报告提交哈希、构建命令、输出目录和剩余风险。

## 最终报告

用中文简要说明：

1. 已完成的设计和代码改动，以及当前仍未完成的事项。
2. 修改文件、素材来源和维护方式。
3. Figma/Canva 是否实际可用；不可用时说明使用的本地替代设计规范。
4. 类型检查、测试、构建、浏览器视口和核心交互证据。
5. `main20260524` 的提交哈希、推送结果和静态托管注意事项。
6. 未解决的隐私、版权、素材来源或响应式风险。

# 作品集部署说明（国内可访问）

## 方案 A（推荐）：腾讯 EdgeOne Pages

适用场景：希望快速上线、支持直接上传静态文件、对国内访问更友好。

### 最快发布步骤（10 分钟）

1. 打开 EdgeOne Pages 控制台并创建项目。
2. 选择 `Direct Upload`（直接上传）。
3. 项目目录上传以下文件与文件夹（保持结构不变）：
   - `index.html`
   - `dashboards.html`
   - `papers.html`
   - `about.html`
   - `styles.css`
   - `script.js`
   - `作品/`
   - `assets/previews/`
4. `Build Command` 留空（纯静态站不需要构建）。
5. 点击 `Start Deployment`。
6. 部署成功后，在项目概览页点击 `Preview` 获取访问链接。

### 域名与国内访问要点

- 若你选择 `Chinese mainland` 或 `Global (including mainland)` 加速区，系统预览链接可访问，但有效期通常是临时的（官方文档提到 3 小时）。
- 要长期稳定分享给 HR，建议绑定自定义域名。
- 自定义域名如要在中国大陆加速，通常需要完成 ICP 备案。

---

## 方案 B（低成本）：阿里云 OSS 静态网站托管

适用场景：预算敏感、希望对象存储托管静态文件。

### 部署步骤

1. 在 OSS 创建 Bucket（存放站点文件）。
2. 开启 `Static website hosting`：
   - Default Homepage: `index.html`
   - Default 404 Page: `index.html`（若按 SPA 方式路由，可用此设置）
3. 上传整个站点文件（同方案 A 的目录结构）。
4. 配置 Bucket 可公网读取（仅对公开资源启用）。
5. 绑定自定义域名并配置 CNAME。
6. 访问域名验证页面可正常打开。

### 域名要点

- OSS 官方文档明确：用 Bucket 默认域名访问 HTML 可能触发下载行为，正常在线浏览建议走自定义域名。
- Bucket 在中国大陆地域时，自定义域名通常需要 ICP 备案。


# 作品集部署说明（国内可访问）

## 方案 A（推荐）：腾讯 EdgeOne Pages

适用场景：希望快速上线、支持 Git 自动构建、对国内访问更友好。

### 最快发布步骤（10 分钟）

1. 将仓库连接到 EdgeOne Pages，分支选择 `main20260524`（或你的发布分支）。
2. 构建命令填写 `npm run build`。
3. 输出目录填写 `dist`。
4. Node.js 环境使用当前 LTS，安装命令可留空或使用 `npm install`。
5. 点击部署，完成后在项目概览页点击 `Preview` 获取访问链接。

站点使用 HashRouter（`#/dashboards`、`#/research` 等），因此无需额外配置 SPA 回退规则。

### 域名与国内访问要点

- 若你选择 `Chinese mainland` 或 `Global (including mainland)` 加速区，系统预览链接可访问，但有效期通常是临时的（官方文档提到 3 小时）。
- 要长期稳定分享给 HR，建议绑定自定义域名。
- 自定义域名如要在中国大陆加速，通常需要完成 ICP 备案。

---

## 方案 B（低成本）：阿里云 OSS 静态网站托管

适用场景：预算敏感、希望对象存储托管静态文件。

### 部署步骤

1. 在 OSS 创建 Bucket（存放站点文件）。
2. 开启 `Static website hosting`：
   - Default Homepage: `index.html`
   - Default 404 Page: `index.html`
3. 本地执行 `npm run build`，上传 `dist/` 内全部文件（不要上传源码、简历、视频或参考截图）。
4. 配置 Bucket 可公网读取（仅对公开资源启用）。
5. 绑定自定义域名并配置 CNAME。
6. 访问域名验证页面可正常打开。

### 域名要点

- OSS 官方文档明确：用 Bucket 默认域名访问 HTML 可能触发下载行为，正常在线浏览建议走自定义域名。
- Bucket 在中国大陆地域时，自定义域名通常需要 ICP 备案。
